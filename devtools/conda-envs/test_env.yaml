name: mdaf3-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # # MDAnalysis
  # - MDAnalysis

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  ##
  - orjson
  - numpy<1.24
  - h5py
  - polars
  - gemmi

  # Pip-only installs
  - pip:
    - "git+https://github.com/marinegor/mdanalysis.git@feature/mmcif#subdirectory=package"

