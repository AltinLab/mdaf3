name: mdaf3-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # # MDAnalysis
  # - MDAnalysis

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  ##
  - orjson
  - numpy
  - h5py
  - polars
  - gemmi

  ## build system (temporary)
  - cython<3.1

  # Pip-only installs
  - pip:
    - "git+https://github.com/ljwoods2/mdanalysis.git@feature/mmcif#subdirectory=package"

