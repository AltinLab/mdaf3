name: mdaf3-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # MDAnalysis
  - MDAnalysis

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  ##
  - orjson
  - numpy
  - h5py
  - polars
  - gemmi

  # Pip-only installs
  - pip:
   - git+https://github.com/MDAnalysis/mdanalysis.git@refs/pull/4712/merge

